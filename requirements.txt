# MCP server + client (official SDK). >=2.0: the code uses the 2.x API (MCPServer, Client).
mcp>=2.0

# LLM over a local OpenAI-compatible endpoint (Ollama now, vLLM later)
openai>=1.40

# API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8

# Utilities
python-dotenv>=1.0
rich>=13.7
